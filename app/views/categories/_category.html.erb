<div id="<%= dom_id category %>" class="category">
  <img src="<%= category.icon %>" class="category-icon">
  <div class='name-container'>
    <div><%= category.name%></div>
    <div><%= category.created_at.strftime("%Y %b %d") %></div>
  </div>
  <div>$<%= category.total_amount %></div>
</div>